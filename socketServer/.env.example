# MongoDB (same DB as the Next.js app)
MONGODB_URL=mongodb+srv://<user>:<password>@<cluster>.mongodb.net/<dbname>

# Server Port
PORT=5000

# Frontend URL (for CORS)
NEXT_BASE_URL=http://localhost:3000
